"""Enrichment stages: optional, network-touching steps that augment reference
data (e.g. geocoding addresses to lat/long). Kept separate from the offline
silver/gold build so the core pipeline never needs the network."""
